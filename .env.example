# Copy this file to .env and adjust the values.
# .env is ignored by git — never commit it.

# Directory where doc2kb writes the Obsidian vault and ChromaDB.
# Defaults to <repo>/output if not set.
# DOC2KB_OUTPUT_DIR=/path/to/your/knowledge-base

# OCR engine priority: Claude Vision > Google Vision > EasyOCR (local)

# Claude Vision — best quality for handwriting (pip install -e ".[ocr-claude]")
# ANTHROPIC_API_KEY=sk-ant-...

# Google Vision — alternative cloud OCR (pip install -e ".[ocr-google]")
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
